import Base
import ./LAWS.bend as Laws

def Laws.get(storage, who, limit, logs):
  {==}

def Laws.increment(storage, who, limit, logs):
  {==}

def Laws.decrement(storage, who, limit, logs):
  {==}

def Laws.set(storage, who, limit, logs, value):
  {==}
