import Base
import ./LAWS.bend as Laws

def Laws.get(storage, who, limit, logs, world):
  {==}

def Laws.increment(storage, who, limit, logs, world):
  {==}

def Laws.decrement(storage, who, limit, logs, world):
  {==}

def Laws.set(storage, who, limit, logs, world, value):
  {==}
