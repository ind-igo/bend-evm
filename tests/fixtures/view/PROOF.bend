import Base
import ../../../src/Evm.bend as Evm
import ./LAWS.bend as Laws

def Laws.others(storage, who, limit, logs, time, chain, self, hashes, signers, q, held, r, supply, rest, token, owner):
  {==}
